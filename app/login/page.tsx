'use client';
import { FormEvent, useState } from 'react';
export default function Login(){ const [email,setEmail]=useState(''); const [password,setPassword]=useState(''); const [error,setError]=useState(''); const submit=async(e:FormEvent)=>{e.preventDefault();setError(''); const r=await fetch('/api/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email,password})}); if(r.ok){location.href='/admin'} else {const x=await r.json().catch(()=>({}));setError(x.error||'No se pudo iniciar sesión.')}}; return <main className="form"><span className="kicker">PEGASUS / ADMIN</span><h1>Iniciar sesión</h1><p className="muted">Panel privado para administrar el catálogo.</p><form onSubmit={submit}><label className="field">Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="username"/></label><label className="field">Contraseña<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<p style={{color:'#e77'}}>{error}</p>}<button className="primary">ENTRAR</button></form></main>}
